package pieces;

public class PieceType { // classe usada para atributos gerais das Peças
    public enum Color {
        PRETA(0),
        BRANCA(1);
        public int color;
        Color(int color) {
            this.color = color;
        }

    }



}
